# Example portraits

These images are demonstration portraits used by Z-Orbit. The public default configuration uses `15-Grok.png`, `04-Claude.png`, and `09-GPT.png` for three website examples. They are not official third-party service logos and do not imply endorsement.

Users can replace portraits from the application management window. Local application paths and personal shortcut files are not distributed.
